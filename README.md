# Spring Boot with Micrometer for Elasticsearch

## Setup

* Disable security features in the `config/elasticsearch.yml` file for testing convenience as follows:

```yaml
xpack.security.enabled: false
```
